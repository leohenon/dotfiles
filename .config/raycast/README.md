# raycast

Scripts live in `scripts/`.

Bootstrap copies these files to `~/.config/raycast`, but Raycast setup is still manual.

You still need to:

- point Script Commands at this folder
- grant macOS permissions as needed
- set any Raycast keybinds in the app
